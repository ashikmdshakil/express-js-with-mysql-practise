<html>
    <head>
        <title>Students List</title>
    </head>
    <body>
        <h1>List of students</h1>
        <% student.forEach((eachStudent) =>{ %>

            <h3><%= eachStudent.name %></h3>
            <h4><%= eachStudent.department %></h4>

        <% }); %>
    </body>
</html>